# 第 03 章 系统流程与时序图

> **内容**: 核心业务流程与交互时序分析
> **图表**: 12 个流程图 + 10 个时序图

---

## 3.1 文档入库流程（Insert Pipeline）

### 3.1.1 流程图

```mermaid
flowchart TD
    Start([用户调用 insert/ainsert]) --> ValidateInput{输入校验}
    ValidateInput -->|失败| Error1[抛出 ValueError]
    ValidateInput -->|成功| Enqueue[文档入队<br/>apipeline_enqueue_documents]

    Enqueue --> ParseDoc[解析文档<br/>Parse Worker]
    ParseDoc --> ParseOK{解析成功?}
    ParseOK -->|失败| MarkFailed1[标记 FAILED<br/>记录错误信息]
    ParseOK -->|成功| ChunkDoc[文本分块<br/>Chunker]

    ChunkDoc --> HasMultimodal{含多模态<br/>内容?}
    HasMultimodal -->|是| AnalyzeMM[多模态分析<br/>Analyze Worker]
    AnalyzeMM --> ExtractEntities[实体/关系抽取<br/>Process Worker]
    HasMultimodal -->|否| ExtractEntities

    ExtractEntities --> MergeKG[图谱合并<br/>merge_nodes_and_edges]
    MergeKG --> EmbedVectors[向量嵌入<br/>EmbeddingFunc]
    EmbedVectors --> WriteStorage[写入存储<br/>KV + Vector + Graph]
    WriteStorage --> MarkDone[标记 PROCESSED<br/>更新状态]

    MarkDone --> Done([文档入库完成])
    MarkFailed1 --> Failed([文档入库失败])

    style Start fill:#4A90D9,color:#fff
    style Done fill:#2ECC71,color:#fff
    style Failed fill:#E74C3C,color:#fff
    style Error1 fill:#E74C3C,color:#fff
```

### 3.1.2 时序图

```mermaid
sequenceDiagram
    participant User as 用户
    participant API as API Server
    participant Core as LightRAG Core
    participant Queue as Pipeline Queue
    participant Parser as Parser
    participant Chunker as Chunker
    participant LLM as LLM Service
    participant Embed as Embedding
    participant KV as KV Storage
    participant VDB as Vector DB
    participant Graph as Graph DB

    User->>API: POST /documents/upload
    API->>Core: ainsert(content, filename)
    Core->>KV: upsert doc_status=PENDING
    Core->>Queue: enqueue document
    Core-->>API: 返回 track_id
    API-->>User: 200 OK {track_id}

    Note over Queue: 异步流水线处理

    Queue->>Core: dequeue batch
    Core->>Parser: parse_document(content)
    Parser-->>Core: ParseResult(chunks, metadata)

    Core->>Chunker: chunk_text(text, strategy)
    Chunker-->>Core: List[TextChunkSchema]

    loop 对每个 Chunk
        Core->>LLM: extract_entities(chunk)
        LLM-->>Core: EntityExtractionResult
        Core->>LLM: extract_relationships(chunk, entities)
        LLM-->>Core: List[Relationship]
    end

    Core->>Core: merge_nodes_and_edges()
    Core->>Embed: embed_entities(entities)
    Embed-->>Core: vectors
    Core->>Embed: embed_relationships(relationships)
    Embed-->>Core: vectors

    Core->>KV: upsert chunks & docs
    Core->>VDB: upsert entity/relation vectors
    Core->>Graph: upsert nodes & edges
    Core->>KV: upsert doc_status=PROCESSED
```

### 3.1.3 详细解释

#### 流程步骤分析

**1. 输入校验阶段**:
- **入口**: `LightRAG.insert()` / `LightRAG.ainsert()`
- **校验内容**: 内容非空、文件名合法、处理选项有效
- **异常处理**: 校验失败抛出 `ValueError`，不进入队列
- **关键函数**: `parse_process_options()` 解析分块和解析配置

**2. 文档入队阶段**:
- **入口**: `_PipelineMixin.apipeline_enqueue_documents()`
- **操作**:
  - 生成文档 ID（`compute_mdhash_id`）
  - 创建初始状态记录（PENDING）
  - 将文档信息写入 `full_docs`
  - 将文档状态写入 `doc_status`
  - 发送入队消息到 Pipeline Queue
- **关键文件**: `lightrag/pipeline.py` 第 359 行

**3. 文档解析阶段**:
- **入口**: `_parse_worker()`
- **处理**:
  - 根据文件后缀选择解析器
  - 调用对应 Parser 提取文本内容
  - 处理多模态内容（图片引用）
- **解析引擎**: Native / MinerU / Docling
- **关键文件**: `lightrag/parser/routing.py`

**4. 文本分块阶段**:
- **策略选择**: 根据 `process_options` 选择分块策略
- **Fix**: 固定 token 大小分块
- **Recursive**: 递归字符分块
- **Vector**: 向量语义分块
- **Paragraph**: 段落语义分块
- **关键文件**: `lightrag/chunker/`

**5. 实体/关系抽取阶段**:
- **入口**: `extract_entities()` in `operate.py`
- **处理**:
  - 对每个 Chunk 调用 LLM 抽取实体
  - 对每个 Chunk 调用 LLM 抽取关系
  - 解析 LLM 返回的 JSON
  - 处理 JSON 修复（`json_repair`）
- **并发控制**: 通过信号量限制并发 LLM 调用
- **关键文件**: `lightrag/operate.py` 第 3559 行

**6. 图谱合并阶段**:
- **入口**: `merge_nodes_and_edges()`
- **处理**:
  - 合并同名实体（描述去重、属性合并）
  - 合并同义边（权重累加）
  - 更新向量数据库
- **关键文件**: `lightrag/operate.py` 第 3222 行

**7. 向量嵌入阶段**:
- **入口**: `EmbeddingFunc.__call__()`
- **处理**:
  - 实体名称 → 向量
  - 关系描述 → 向量
  - 文本块内容 → 向量
- **批量处理**: 支持批量嵌入提高效率

**8. 存储写入阶段**:
- **KV Storage**: 写入 `text_chunks`、`full_docs`
- **Vector DB**: 写入 `entities`、`relationships`、`chunks`
- **Graph DB**: 写入节点和边
- **DocStatus**: 更新为 PROCESSED

#### 异常处理

| 异常类型 | 处理方式 | 结果状态 |
|----------|----------|----------|
| 解析失败 | 记录错误，标记 FAILED | FAILED |
| LLM 调用超时 | 重试（tenacity），超过重试次数标记 FAILED | FAILED |
| JSON 解析失败 | 尝试 `json_repair`，仍失败则跳过该 Chunk | 部分成功 |
| 存储写入失败 | 抛出 `IndexFlushError`，批次回滚 | FAILED |
| 流水线取消 | 捕获 `PipelineCancelledException`，优雅终止 | CANCELLED |

---

## 3.2 知识图谱构建流程（KG Construction）

### 3.2.1 流程图

```mermaid
flowchart TD
    Start([开始 KG 构建]) --> GetChunks[获取所有文本块<br/>从 text_chunks KV]
    GetChunks --> LoopStart{还有未处理的<br/>Chunk?}

    LoopStart -->|是| GetOneChunk[取出一个 Chunk]
    GetOneChunk --> CallLLM[调用 LLM 抽取<br/>实体 + 关系]
    CallLLM --> ParseResult[解析 LLM 返回<br/>JSON → 结构化数据]

    ParseResult --> ValidResult{结果有效?}
    ValidResult -->|否| SkipChunk[跳过该 Chunk<br/>记录警告日志]
    ValidResult -->|是| StoreRaw[存储原始抽取结果<br/>full_entities/full_relations]

    StoreRaw --> LoopStart

    LoopStart -->|否| MergeStart[开始图谱合并]
    MergeStart --> GroupEntities[按实体名称分组<br/>跨文档聚合]
    GroupEntities --> MergeEntities[合并同名实体<br/>描述去重、属性合并]
    MergeEntities --> GroupRelations[按关系对分组<br/>(src, tgt) 聚合]
    GroupRelations --> MergeRelations[合并同义边<br/>权重累加、描述合并]

    MergeRelations --> EmbedPhase[向量嵌入阶段]
    EmbedPhase --> EmbedEntities[实体名称 → 向量]
    EmbedEntities --> EmbedRelations[关系描述 → 向量]

    EmbedRelations --> WriteVDB[写入向量数据库<br/>entities_vdb + relationships_vdb]
    WriteVDB --> WriteGraph[写入图数据库<br/>节点 + 边]
    WriteGraph --> Done([KG 构建完成])

    style Start fill:#4A90D9,color:#fff
    style Done fill:#2ECC71,color:#fff
    style SkipChunk fill:#F39C12,color:#fff
```

### 3.2.2 时序图

```mermaid
sequenceDiagram
    participant Core as KG Builder
    participant Chunks as text_chunks KV
    participant LLM as LLM Service
    participant Cache as LLM Cache
    participant RawKV as full_entities/relations
    participant Merger as Merge Engine
    participant Embed as Embedding
    participant VDB as Vector DB
    participant Graph as Graph DB

    Core->>Chunks: get_all_chunks()
    Chunks-->>Core: List[TextChunkSchema]

    loop 对每个 Chunk（并发受信号量控制）
        Core->>Cache: check_cache(chunk_hash)
        Cache-->>Core: cache_hit / cache_miss

        alt cache_miss
            Core->>LLM: extract(chunk_content)
            LLM-->>Core: JSON response
            Core->>Cache: save_to_cache(result)
        end

        Core->>Core: parse_extraction_result()
        Core->>RawKV: upsert(entity/relation records)
    end

    Core->>RawKV: get_all_entities()
    RawKV-->>Core: Dict[name, List[records]]
    Core->>Merger: merge_nodes_then_upsert()
    Merger-->>Core: merged_nodes

    Core->>RawKV: get_all_relations()
    RawKV-->>Core: Dict[(src,tgt), List[records]]
    Core->>Merger: merge_edges_then_upsert()
    Merger-->>Core: merged_edges

    loop 对每个合并后的实体
        Core->>Embed: embed(entity_name)
        Embed-->>Core: vector
        Core->>VDB: upsert_entity(id, vector, data)
    end

    loop 对每个合并后的关系
        Core->>Embed: embed(relation_desc)
        Embed-->>Core: vector
        Core->>VDB: upsert_relation(id, vector, data)
    end

    Core->>Graph: batch_upsert_nodes(merged_nodes)
    Core->>Graph: batch_upsert_edges(merged_edges)
```

### 3.2.3 详细解释

#### 实体抽取详细流程

**1. 提示词构建**:
- 使用 `prompt.py` 中的 `entity_extraction` 模板
- 包含：实体类型定义、输出格式示例、待抽取文本
- 支持多语言（中文/英文）

**2. LLM 调用**:
- 使用 EXTRACT 角色配置的 LLM
- 支持缓存（相同输入直接返回缓存结果）
- 超时控制（`DEFAULT_LLM_TIMEOUT`）

**3. 结果解析**:
- 尝试 JSON 解析
- 失败时使用 `json_repair` 修复
- 使用 Pydantic 模型校验（`EntityExtractionResult`）

**4. 实体归一化**:
- 名称标准化（大小写、空格、特殊字符）
- 类型标准化（映射到预定义类型）
- 描述去重与截断

#### 图谱合并算法

**实体合并**:
```
输入: 同名实体的多个描述
处理:
  1. 描述去重（语义相似度 > 阈值则合并）
  2. 属性合并（来源文档列表合并）
  3. 权重更新（出现次数累加）
输出: 合并后的单一实体
```

**关系合并**:
```
输入: 同对实体的多个关系
处理:
  1. 关键词合并（去重）
  2. 描述合并（去重）
  3. 权重累加
输出: 合并后的单一关系
```

---

## 3.3 查询处理流程（Query Pipeline）

### 3.3.1 流程图

```mermaid
flowchart TD
    Start([用户提交查询]) --> ParseParam[解析查询参数<br/>QueryParam]
    ParseParam --> RouteMode{选择查询模式}

    RouteMode -->|local| LocalPath[局部检索路径]
    RouteMode -->|global| GlobalPath[全局检索路径]
    RouteMode -->|hybrid| HybridPath[混合检索路径]
    RouteMode -->|naive| NaivePath[朴素检索路径]
    RouteMode -->|mix| MixPath[融合检索路径]
    RouteMode -->|bypass| BypassPath[直接 LLM 路径]

    subgraph LocalFlow["Local 模式"]
        LocalPath --> L1[关键词抽取<br/>LLM 提取实体]
        L1 --> L2[实体向量检索<br/>Top-K 相似实体]
        L2 --> L3[获取实体关联块<br/>从 Graph 获取]
        L3 --> L4[上下文组装<br/>Token 控制]
    end

    subgraph GlobalFlow["Global 模式"]
        GlobalPath --> G1[关键词抽取<br/>LLM 提取主题]
        G1 --> G2[关系向量检索<br/>Top-K 相似关系]
        G2 --> G3[获取关系详情<br/>从 Graph 获取]
        G3 --> G4[上下文组装<br/>Token 控制]
    end

    subgraph MixFlow["Mix 模式"]
        MixPath --> M1[关键词抽取]
        M1 --> M2[实体 + 关系<br/>向量检索]
        M2 --> M3[关联块获取]
        M3 --> M4[重排序<br/>Reranker]
        M4 --> M5[上下文组装]
    end

    subgraph NaiveFlow["Naive 模式"]
        NaivePath --> N1[查询向量化<br/>EmbeddingFunc]
        N1 --> N2[块向量检索<br/>Top-K 相似块]
        N2 --> N3[上下文组装]
    end

    L4 --> Rerank{启用<br/>Reranker?}
    G4 --> Rerank
    M5 --> Rerank
    N3 --> Rerank

    Rerank -->|是| DoRerank[重排序<br/>相关性打分]
    Rerank -->|否| GenPrompt[生成最终提示词<br/>上下文 + 查询]

    DoRerank --> GenPrompt
    BypassPath --> GenPrompt

    GenPrompt --> CallLLM[调用 LLM 生成<br/>QUERY 角色]
    CallLLM --> StreamCheck{流式输出?}
    StreamCheck -->|是| StreamOut[SSE 流式返回]
    StreamCheck -->|否| NormalOut[完整响应返回]

    StreamOut --> Done([查询完成])
    NormalOut --> Done

    style Start fill:#4A90D9,color:#fff
    style Done fill:#2ECC71,color:#fff
    style RouteMode fill:#F39C12,color:#fff
```

### 3.3.2 时序图

```mermaid
sequenceDiagram
    participant User as 用户
    participant API as API Server
    participant Core as Query Engine
    participant KW as Keyword Extractor
    participant VDB as Vector DB
    participant Graph as Graph DB
    participant KV as KV Storage
    participant Rank as Reranker
    participant LLM as LLM Service

    User->>API: POST /query {query, mode}
    API->>Core: aquery(query, QueryParam)

    Core->>KW: extract_keywords(query)
    KW-->>Core: {entities: [], keywords: []}

    alt local / mix 模式
        loop 对每个实体
            Core->>VDB: query(entity_vector, top_k)
            VDB-->>Core: similar_entities
            Core->>Graph: get_node_edges(entity_id)
            Graph-->>Core: related_chunks
        end
    end

    alt global / mix 模式
        loop 对每个关键词
            Core->>VDB: query(keyword_vector, top_k)
            VDB-->>Core: similar_relations
            Core->>Graph: get_edge_details(relation_id)
            Graph-->>Core: relation_context
        end
    end

    Core->>Core: assemble_context()
    Note over Core: Token 控制截断

    alt 启用 Reranker
        Core->>Rank: rerank(query, contexts)
        Rank-->>Core: ranked_contexts
    end

    Core->>Core: build_prompt(context, query)
    Core->>LLM: generate(prompt)
    LLM-->>Core: response

    Core->>Core: update_cache()
    Core-->>API: QueryResult
    API-->>User: 200 OK {response, contexts}
```

### 3.3.3 详细解释

#### 查询模式详解

**Local 模式**:
- **核心思想**: 基于查询中的实体进行局部图遍历
- **适用场景**: 特定实体相关问题（如"Python 的作者是谁？"）
- **检索范围**: 实体节点 → 关联边 → 关联块
- **优势**: 精确度高，上下文紧凑

**Global 模式**:
- **核心思想**: 基于全局知识图谱的广泛检索
- **适用场景**: 需要全局知识的问题（如"总结本文档的主要内容"）
- **检索范围**: 关系节点 → 全局主题
- **优势**: 覆盖面广，适合总结类问题

**Hybrid 模式**:
- **核心思想**: Local + Global 的简单组合
- **适用场景**: 平衡局部与全局
- **实现**: 分别执行两种检索，合并结果

**Mix 模式（推荐）**:
- **核心思想**: 知识图谱 + 向量检索深度融合
- **适用场景**: 通用场景，默认推荐
- **特点**: 包含重排序，检索质量最高

**Naive 模式**:
- **核心思想**: 纯向量检索，无图谱增强
- **适用场景**: 基线对比、简单查询
- **实现**: 查询向量化 → 块向量检索 → 上下文组装

**Bypass 模式**:
- **核心思想**: 直接 LLM 回答，无检索
- **适用场景**: 通用知识问题（如"什么是机器学习？"）
- **实现**: 直接调用 LLM，不检索任何上下文

#### Token 控制机制

LightRAG 使用统一的 Token 控制机制管理上下文大小：

```python
@dataclass
class QueryParam:
    max_entity_tokens: int = 8192    # 实体上下文最大 token
    max_relation_tokens: int = 8192  # 关系上下文最大 token
    max_total_tokens: int = 30000    # 总上下文最大 token
```

**截断策略**:
1. 按 Top-K 检索结果排序
2. 逐个添加到上下文
3. 达到 token 上限时截断
4. 优先保留高相关性内容

---

## 3.4 分块策略流程（Chunking Strategies）

### 3.4.1 流程图

```mermaid
flowchart TD
    Start([输入文本]) --> SelectStrategy{选择分块策略}

    SelectStrategy -->|Fix| FixPath[固定大小分块]
    SelectStrategy -->|Recursive| RecPath[递归字符分块]
    SelectStrategy -->|Vector| VecPath[向量语义分块]
    SelectStrategy -->|Paragraph| ParaPath[段落语义分块]

    subgraph FixDetail["Fix 策略"]
        FixPath --> F1[按固定 Token 大小切分<br/>默认 1200 tokens]
        F1 --> F2[可选重叠区域<br/>chunk_overlap]
        F2 --> F3[生成 TextChunkSchema<br/>含 chunk_order_index]
    end

    subgraph RecDetail["Recursive 策略"]
        RecPath --> R1[递归字符切分<br/>langchain-text-splitters]
        R1 --> R2[切分符优先级<br/>\\n\\n → \\n → . → 空格]
        R2 --> R3[保持段落完整性]
    end

    subgraph VecDetail["Vector 策略"]
        VecPath --> V1[句子级别切分]
        V1 --> V2[计算相邻句子<br/>语义相似度]
        V2 --> V3[相似度低于阈值<br/>则切分]
        V3 --> V4[生成语义完整的块]
    end

    subgraph ParaDetail["Paragraph 策略"]
        ParaPath --> P1[SPACY 段落识别]
        P1 --> P2[计算段落间<br/>语义向量]
        P2 --> P3[相似度聚类<br/>合并相关段落]
        P3 --> P4[生成段落级块]
    end

    F3 --> Output([输出块列表])
    R3 --> Output
    V4 --> Output
    P4 --> Output

    style Start fill:#4A90D9,color:#fff
    style Output fill:#2ECC71,color:#fff
    style SelectStrategy fill:#F39C12,color:#fff
```

### 3.4.2 详细解释

#### 四种分块策略对比

| 维度 | Fix | Recursive | Vector | Paragraph |
|------|-----|-----------|--------|-----------|
| **切分单位** | 固定 Token | 递归字符 | 语义相似度 | 段落 |
| **语义保持** | 弱 | 中 | 强 | 强 |
| **计算开销** | 极低 | 低 | 高（需 Embedding） | 高（需 SPACY + Embedding） |
| **块大小均匀** | 是 | 较均匀 | 不均匀 | 不均匀 |
| **依赖** | tiktoken | langchain | 嵌入模型 | spacy + 嵌入模型 |
| **适用场景** | 通用 | 长文档 | 语义敏感 | 结构化文档 |

#### Fix 策略实现

**文件**: `lightrag/chunker/token_size.py`

**核心逻辑**:
1. 使用 tokenizer 将文本转为 token 列表
2. 按 `chunk_token_size` 切分
3. 可选 `chunk_overlap` 重叠区域
4. 生成 `TextChunkSchema`（含 tokens, content, full_doc_id, chunk_order_index）

#### Recursive 策略实现

**文件**: `lightrag/chunker/recursive_character.py`

**核心逻辑**:
1. 使用 `langchain-text-splitters` 的 `RecursiveCharacterTextSplitter`
2. 切分符优先级：`\n\n` → `\n` → `.` → ` ` → ``
3. 保持段落和句子的完整性

#### Vector 策略实现

**文件**: `lightrag/chunker/semantic_vector.py`

**核心逻辑**:
1. 将文本切分为句子
2. 计算相邻句子的语义相似度
3. 相似度低于阈值则切分
4. 生成语义完整的块

#### Paragraph 策略实现

**文件**: `lightrag/chunker/paragraph_semantic.py`（106,286 行，最大文件）

**核心逻辑**:
1. 使用 SPACY 进行段落识别
2. 计算段落间语义向量
3. 相似度聚类合并相关段落
4. 生成段落级块

---

## 3.5 文档解析流程（Document Parsing）

### 3.5.1 流程图

```mermaid
flowchart TD
    Start([原始文档输入]) --> DetectType{检测文件类型<br/>按后缀名}

    DetectType -->|PDF| PDFPath[PDF 解析]
    DetectType -->|DOCX| DOCXPath[DOCX 解析]
    DetectType -->|PPTX| PPTXPath[PPTX 解析]
    DetectType -->|XLSX| XLSXPath[XLSX 解析]
    DetectType -->|MD/HTML| MDPath[Markdown/HTML 解析]
    DetectType -->|TXT| TXTPath[纯文本解析]

    subgraph EngineSelect["解析引擎选择"]
        PDFPath --> Engine{选择引擎}
        DOCXPath --> Engine
        PPTXPath --> Engine
        Engine -->|Native| NativeEng[内置解析器<br/>纯 Python 实现]
        Engine -->|MinerU| MinerUEng[MinerU 服务<br/>PDF 专家]
        Engine -->|Docling| DoclingEng[Docling 服务<br/>IBM 开源]
    end

    NativeEng --> ExtractContent[提取文本内容<br/>+ 结构信息 + 图片引用]
    MinerUEng --> ExtractContent
    DoclingEng --> ExtractContent

    ExtractContent --> HasImages{含图片?}
    HasImages -->|是| ExtractImages[提取图片<br/>保存到 parsed 目录]
    HasImages -->|否| GenResult[生成 ParseResult]

    ExtractImages --> GenResult
    GenResult --> Done([解析完成])

    style Start fill:#4A90D9,color:#fff
    style Done fill:#2ECC71,color:#fff
    style Engine fill:#F39C12,color:#fff
```

### 3.5.2 时序图

```mermaid
sequenceDiagram
    participant Pipeline as Pipeline
    participant Router as Parser Router
    participant Registry as Parser Registry
    participant Parser as Concrete Parser
    participant MM as Multimodal Extractor
    participant FS as File System

    Pipeline->>Router: resolve_parser_engine(filename, options)
    Router->>Registry: get_parser(suffix, engine)
    Registry-->>Router: parser_class
    Router-->>Pipeline: parser_instance

    Pipeline->>Parser: parse_document(content, context)
    Parser->>Parser: detect_encoding()

    alt PDF 文件
        Parser->>Parser: extract_text_by_page()
        Parser->>Parser: extract_images_from_pdf()
    else DOCX 文件
        Parser->>Parser: extract_paragraphs()
        Parser->>Parser: extract_tables()
        Parser->>Parser: extract_images_from_docx()
    else Markdown
        Parser->>Parser: parse_markdown_structure()
    end

    Parser->>MM: extract_multimodal_refs()
    MM-->>Parser: List[ImageRef]

    loop 对每张图片
        Parser->>FS: save_image_to_parsed_dir()
    end

    Parser-->>Pipeline: ParseResult(text, images, metadata)
```

### 3.5.3 详细解释

#### 解析器架构

**Parser Registry**（`lightrag/parser/registry.py`）:
- 维护文件后缀 → 解析器类的映射
- 支持插件化注册第三方解析器
- 提供 `get_parser()` 统一入口

**Parser Base**（`lightrag/parser/base.py`）:
- 定义 `parse_document()` 抽象方法
- 提供 `ParseContext` 上下文对象
- 支持解析选项传递

**Native Parser**（`lightrag/parser/native_base.py`）:
- 纯 Python 实现，无外部依赖
- 支持：PDF (pypdf)、DOCX (python-docx)、PPTX (python-pptx)、MD、HTML、TXT

**External Parser**:
- **MinerU**: PDF 专业解析，支持复杂排版
- **Docling**: IBM 开源，支持多种格式
- 通过 HTTP 服务调用

---

## 3.6 多模态处理流程（Multimodal Pipeline）

### 3.6.1 流程图

```mermaid
flowchart TD
    Start([文档含多模态内容]) --> ParseDoc[文档解析<br/>提取图片引用]
    ParseDoc --> LoopStart{还有未处理的<br/>图片?}

    LoopStart -->|是| GetImage[获取一张图片]
    GetImage --> CheckCache{缓存命中?}

    CheckCache -->|是| UseCache[使用缓存结果]
    CheckCache -->|否| CallVLM[调用 VLM 分析<br/>Vision LLM]

    CallVLM --> VLMResult{VLM 返回<br/>成功?}
    VLMResult -->|失败| RetryCount{重试次数<br/>未耗尽?}
    RetryCount -->|是| CallVLM
    RetryCount -->|否| MarkFailed[标记分析失败]
    VLMResult -->|成功| SaveResult[保存分析结果<br/>sidecar 文件]

    SaveResult --> UpdateCache[更新缓存]
    UpdateCache --> LoopStart
    MarkFailed --> LoopStart

    LoopStart -->|否| MergeResult[合并多模态<br/>分析结果到文档]
    MergeResult --> Done([多模态处理完成])

    style Start fill:#4A90D9,color:#fff
    style Done fill:#2ECC71,color:#fff
    style MarkFailed fill:#E74C3C,color:#fff
```

### 3.6.2 详细解释

#### 多模态处理架构

**VLM 调用**:
- 使用 VLM 角色配置的 LLM（通常是 GPT-4V 或 Gemini Pro Vision）
- 输入：图片 + 分析提示词
- 输出：图片描述、表格数据、公式识别

**Sidecar 机制**:
- 多模态分析结果存储在 `.sidecar.jsonl` 文件
- 与原始文档关联
- 支持增量更新

**缓存策略**:
- 基于图片内容的哈希值缓存
- 相同图片不重复分析
- 减少 API 调用成本

---

## 3.7 缓存机制流程（LLM Cache）

### 3.7.1 流程图

```mermaid
flowchart TD
    Start([LLM 调用请求]) --> GenCacheKey[生成缓存键<br/>compute_args_hash]
    GenCacheKey --> CheckCache{缓存存在?}

    CheckCache -->|是| CheckValid{缓存有效?<br/>未过期}
    CheckValid -->|是| CacheHit[缓存命中<br/>直接返回]
    CheckValid -->|否| CallLLM[调用 LLM]

    CheckCache -->|否| CallLLM
    CallLLM --> LLMSuccess{调用成功?}

    LLMSuccess -->|是| SaveCache[保存到缓存<br/>KV Storage]
    LLMSuccess -->|否| RetryCheck{重试次数<br/>未耗尽?}
    RetryCheck -->|是| CallLLM
    RetryCheck -->|否| ErrorOut[返回错误]

    SaveCache --> ReturnResult[返回结果]
    CacheHit --> ReturnResult
    ReturnResult --> Done([完成])

    style Start fill:#4A90D9,color:#fff
    style Done fill:#2ECC71,color:#fff
    style CacheHit fill:#27AE60,color:#fff
    style ErrorOut fill:#E74C3C,color:#fff
```

### 3.7.2 详细解释

#### 缓存键生成

```python
def generate_cache_key(mode: str, cache_type: str, hash_value: str) -> str:
    return f"{mode}_{cache_type}_{hash_value}"
```

**缓存类型**:
- `extract`: 实体/关系抽取缓存
- `query`: 查询回答缓存
- `keywords`: 关键词抽取缓存

**哈希计算**:
- 使用 `compute_args_hash()` 对参数进行哈希
- 包含：查询文本、模式、Top-K 等参数
- 确保相同参数生成相同缓存键

#### 缓存存储

- **存储位置**: `llm_response_cache`（KV Storage）
- **存储内容**: `CacheData`（response, creation_time, mode, cache_type）
- **过期策略**: 基于 TTL 或手动清理
- **缓存清理**: `aclear_cache()` 方法

---

## 3.8 文档删除与 KG 重建流程

### 3.8.1 流程图

```mermaid
flowchart TD
    Start([删除文档请求]) --> GetDoc[获取文档信息<br/>从 full_docs]
    GetDoc --> GetChunks[获取文档关联块<br/>从 text_chunks]

    GetChunks --> PurgeChunks[清理块数据<br/>从 chunks_vdb 删除]
    PurgeChunks --> PurgeKG[清理 KG 贡献<br/>_purge_kg_contributions]

    subgraph KGPurge["KG 贡献清理"]
        PurgeKG --> FindEntities[查找文档贡献的实体]
        FindEntities --> UpdateEntities[更新实体描述<br/>移除该文档来源]
        UpdateEntities --> CheckEntityEmpty{实体无其他<br/>来源?}
        CheckEntityEmpty -->|是| DeleteEntity[删除实体<br/>从 Graph + VDB]
        CheckEntityEmpty -->|否| KeepEntity[保留实体<br/>更新描述]

        FindEntities --> FindRelations[查找文档贡献的关系]
        FindRelations --> UpdateRelations[更新关系描述]
        UpdateRelations --> CheckRelationEmpty{关系无其他<br/>来源?}
        CheckRelationEmpty -->|是| DeleteRelation[删除关系]
        CheckRelationEmpty -->|否| KeepRelation[保留关系]
    end

    DeleteEntity --> CleanStatus[清理文档状态<br/>从 doc_status 删除]
    KeepEntity --> CleanStatus
    DeleteRelation --> CleanStatus
    KeepRelation --> CleanStatus

    CleanStatus --> Done([删除完成])

    style Start fill:#4A90D9,color:#fff
    style Done fill:#2ECC71,color:#fff
    style DeleteEntity fill:#E74C3C,color:#fff
    style DeleteRelation fill:#E74C3C,color:#fff
```

### 3.8.2 时序图

```mermaid
sequenceDiagram
    participant User as 用户
    participant Core as LightRAG
    participant KV as KV Storage
    participant VDB as Vector DB
    participant Graph as Graph DB

    User->>Core: adelete_by_doc_id(doc_id)
    Core->>KV: get_by_id(doc_id)
    KV-->>Core: doc_info

    Core->>KV: get_doc_chunks(doc_id)
    KV-->>Core: List[chunk_ids]

    Core->>VDB: delete(chunk_ids)
    Core->>KV: delete(chunk_ids)

    Core->>Core: _purge_kg_contributions(doc_id)
    Note over Core: 遍历文档贡献的实体和关系

    loop 对每个实体
        Core->>Graph: get_node(entity_id)
        Graph-->>Core: node_data
        Core->>Core: remove_doc_from_sources()

        alt 实体无其他来源
            Core->>Graph: delete_node(entity_id)
            Core->>VDB: delete(entity_id)
        else 实体有其他来源
            Core->>Graph: upsert_node(updated_data)
        end
    end

    Core->>KV: delete(doc_id)
    Core->>KV: delete_doc_status(doc_id)
    Core-->>User: DeletionResult
```

### 3.8.3 详细解释

#### 删除策略

**级联删除**:
1. 删除文档本身（`full_docs`）
2. 删除文档的文本块（`text_chunks` + `chunks_vdb`）
3. 清理知识图谱贡献（实体/关系）
4. 删除文档状态（`doc_status`）

**KG 贡献清理**:
- 实体：移除该文档的来源引用，若无其他来源则删除实体
- 关系：同上
- 向量：同步删除对应的向量记录

---

## 3.9 认证与权限流程（Auth Flow）

### 3.9.1 时序图

```mermaid
sequenceDiagram
    participant User as 用户
    participant API as API Server
    participant AuthMW as Auth Middleware
    participant AuthSvc as Auth Service
    participant KV as Token Store

    Note over User,KV: 登录流程
    User->>API: POST /auth/login {username, password}
    API->>AuthSvc: authenticate(username, password)
    AuthSvc->>KV: get_user(username)
    KV-->>AuthSvc: {password_hash, roles}
    AuthSvc->>AuthSvc: bcrypt.verify(password, password_hash)
    AuthSvc->>AuthSvc: generate_jwt_token(user)
    AuthSvc-->>API: {token, expires}
    API-->>User: 200 OK {token}

    Note over User,KV: 请求认证流程
    User->>API: GET /documents/list Authorization: Bearer <token>
    API->>AuthMW: validate_token(token)
    AuthMW->>AuthMW: jwt.decode(token, secret)
    AuthMW->>AuthMW: check_token_expiry()
    AuthMW->>AuthMW: extract_user_roles()

    alt Token 有效
        AuthMW->>API: set_request_user(user)
        API->>API: process_request()
        API-->>User: 200 OK
    else Token 无效/过期
        AuthMW-->>API: 401 Unauthorized
        API-->>User: 401 {error: "Invalid token"}
    end
```

### 3.9.2 详细解释

#### 认证机制

**API Key 认证**:
- 请求头：`X-API-Key: your-api-key`
- 适用于服务间调用
- 配置：`LIGHTRAG_API_KEY` 环境变量

**JWT Token 认证**:
- 登录后获取 Token
- 请求头：`Authorization: Bearer <token>`
- Token 包含：用户 ID、角色、过期时间
- 签名算法：HS256

**密码存储**:
- 使用 bcrypt 哈希
- 工作因子：12（默认）
- 不支持明文存储

#### 权限控制

**角色类型**:
- `admin`: 完全控制
- `user`: 基本操作（查询、上传）
- `readonly`: 只读访问

**权限粒度**:
- 路由级别：通过依赖注入控制
- 工作空间级别：多租户隔离

---

## 3.10 存储后端初始化流程

### 3.10.1 流程图

```mermaid
flowchart TD
    Start([LightRAG 实例创建]) --> PostInit[__post_init__<br/>初始化配置]
    PostInit --> BuildConfig[构建全局配置<br/>_build_global_config]
    BuildConfig --> InitStorages[initialize_storages<br/>异步初始化]

    subgraph StorageInit["存储初始化"]
        InitStorages --> CreateKV[创建 KV Storage<br/>工厂模式]
        CreateKV --> InitKV[KV.initialize()<br/>建立连接/加载文件]
        InitKV --> CreateVector[创建 Vector Storage]
        InitVector --> InitVectorDB[Vector.initialize()<br/>创建索引]
        InitVectorDB --> CreateGraph[创建 Graph Storage]
        InitGraph --> InitGraphDB[Graph.initialize()<br/>加载图数据]
        InitGraphDB --> CreateDocStatus[创建 DocStatus Storage]
        InitDocStatus --> InitDocStatusDB[DocStatus.initialize()]
    end

    InitDocStatusDB --> PipelineInit[初始化流水线状态<br/>initialize_pipeline_status]
    PipelineInit --> RegisterNS[注册命名空间<br/>namespace isolation]
    RegisterNS --> Ready([系统就绪])

    style Start fill:#4A90D9,color:#fff
    style Ready fill:#2ECC71,color:#fff
```

### 3.10.2 详细解释

#### 初始化顺序

1. **配置构建**: 合并环境变量、配置文件、构造参数
2. **KV Storage 初始化**: 最先初始化，其他存储可能依赖
3. **Vector Storage 初始化**: 创建向量索引
4. **Graph Storage 初始化**: 加载图数据
5. **DocStatus Storage 初始化**: 加载文档状态
6. **Pipeline 状态初始化**: 注册命名空间，准备接收文档

#### 命名空间隔离

```python
class NameSpace:
    KV_STORE_FULL_DOCS = "full_docs"
    KV_STORE_TEXT_CHUNKS = "text_chunks"
    VECTOR_STORE_ENTITIES = "entities"
    GRAPH_STORE_CHUNK_ENTITY_RELATION = "chunk_entity_relation"
    DOC_STATUS = "doc_status"
```

- 每个 LightRAG 实例有独立的命名空间
- 多工作空间支持（`workspace` 参数）
- 命名空间用于存储键前缀，实现数据隔离

---

## 3.11 重排序流程（Reranker）

### 3.11.1 流程图

```mermaid
flowchart TD
    Start([检索结果输入]) --> RerankCheck{启用 Reranker?}
    RerankCheck -->|否| DirectReturn[直接返回<br/>原始排序]
    RerankCheck -->|是| CallRerank[调用 Reranker<br/>相关性打分]

    CallRerank --> ScoreResults[对每个候选<br/>计算相关性分数]
    ScoreResults --> SortByScore[按分数降序排序]
    SortByScore --> TopKSelect[取 Top-K 结果]
    TopKSelect --> ReturnReranked[返回重排序结果]

    DirectReturn --> Done([完成])
    ReturnReranked --> Done

    style Start fill:#4A90D9,color:#fff
    style Done fill:#2ECC71,color:#fff
    style CallRerank fill:#F39C12,color:#fff
```

### 3.11.2 详细解释

#### Reranker 架构

**抽象接口** (`lightrag/rerank.py`):
```python
class BaseReranker(ABC):
    @abstractmethod
    async def rerank(self, query: str, documents: list) -> list[tuple[str, float]]:
        ...
```

**实现方式**:
- **Jina Reranker**: 调用 Jina AI API
- **Cohere Reranker**: 调用 Cohere API
- **本地模型**: 使用 HuggingFace 模型

**集成点**:
- 在 `kg_query()` 函数中调用
- 在上下文组装前执行
- 可配置开关（`enable_rerank` 参数）

---

## 3.12 实体编辑与合并流程

### 3.12.1 流程图

```mermaid
flowchart TD
    Start([实体编辑请求]) --> EditType{操作类型}

    EditType -->|编辑| EditEntity[编辑实体<br/>aedit_entity]
    EditType -->|合并| MergeEntity[合并实体<br/>amerge_entities]
    EditType -->|创建| CreateEntity[创建实体<br/>acreate_entity]
    EditType -->|删除| DeleteEntity[删除实体<br/>adelete_by_entity]

    subgraph EditFlow["编辑流程"]
        EditEntity --> GetEntity[获取实体信息]
        GetEntity --> UpdateFields[更新字段<br/>描述/类型/属性]
        UpdateFields --> SaveEntity[保存到 Graph + VDB]
    end

    subgraph MergeFlow["合并流程"]
        MergeEntity --> GetSources[获取源实体列表]
        GetSources --> SelectTarget[选择目标实体]
        SelectTarget --> MergeDescs[合并描述<br/>去重 + 拼接]
        MergeDescs --> MergeAttrs[合并属性<br/>来源列表合并]
        MergeAttrs --> TransferEdges[转移边<br/>源→目标]
        TransferEdges --> DeleteSources[删除源实体]
        DeleteSources --> SaveTarget[保存目标实体]
    end

    SaveEntity --> Done([完成])
    SaveTarget --> Done
    CreateEntity --> Done
    DeleteEntity --> Done

    style Start fill:#4A90D9,color:#fff
    style Done fill:#2ECC71,color:#fff
```

### 3.12.2 详细解释

#### 实体合并算法

**输入**: 多个同名或同义实体
**处理**:
1. 选择主实体（通常是出现次数最多的）
2. 合并描述（去重、截断）
3. 合并属性（来源文档列表合并）
4. 转移边（所有关联边转移到主实体）
5. 删除冗余实体
6. 更新向量数据库

**关键函数**: `amerge_entities()` in `lightrag/lightrag.py`

---

## 3.13 本章小结

本章对 LightRAG 的核心业务流程进行了全面分析：

| 流程 | 关键入口 | 核心步骤 | 输出 |
|------|----------|----------|------|
| **文档入库** | `ainsert()` | 入队→解析→分块→抽取→合并→入库 | PROCESSED 状态 |
| **KG 构建** | `extract_entities()` | 抽取→合并→嵌入→写入 | 知识图谱 |
| **查询处理** | `aquery()` | 路由→检索→重排序→生成 | 回答 + 上下文 |
| **分块策略** | `Chunker` | 4 种策略可选 | 文本块列表 |
| **文档解析** | `Parser` | 多引擎→多格式 | 结构化文本 |
| **多模态** | `Analyze Worker` | VLM 分析→sidecar | 图片描述 |
| **缓存** | `handle_cache()` | 哈希→查询→存储 | 缓存命中/未命中 |
| **文档删除** | `adelete_by_doc_id()` | 级联清理 | 删除结果 |
| **认证** | `Auth MW` | JWT 验证→角色提取 | 认证结果 |
| **存储初始化** | `initialize_storages()` | 工厂→初始化→注册 | 就绪状态 |
| **重排序** | `Reranker` | 打分→排序→截断 | 重排序结果 |
| **实体编辑** | `aedit_entity()` | 获取→更新→保存 | 编辑结果 |

**关键设计模式**:
- **生产者-消费者**: Pipeline 文档处理
- **策略模式**: 查询模式、分块策略
- **模板方法**: 存储后端抽象
- **观察者模式**: 状态变更通知

下一章将深入分析模块结构和依赖关系。

---

> **下一章**: [04-模块结构与依赖分析.md](./04-模块结构与依赖分析.md)

---

☕️ 制作不易，请我喝咖啡☕️关注我➕

![promotion](../res/promotion.jpg)